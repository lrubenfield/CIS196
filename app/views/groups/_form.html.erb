<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
	<%= f.label :name,
	class: 'control-label' %>
	<div class="controls">
	<%= f.text_field :name  %>
		</div>
  
  <div class="control-group">
	<%= f.label :school,
	class: 'control-label' %>
	<div class="controls">
	<%= f.text_field :school  %>
		</div>
  
  <div class="control-group">
  	<%= f.label :talent, class: 'control-label' %>
  	<label class="checkbox">
  	<%= check_box_tag(:talent) %>
	<%= label_tag(:talent, "Dance") %>
	</label>
	<label class="checkbox">
	<%= check_box_tag(:talent) %>
	<%= label_tag(:talent, "Sing") %>
	</label>
	<label class="checkbox">
	<%= check_box_tag(:talent) %>
	<%= label_tag(:talent, "Cook") %>
	</label>
	<label class="checkbox">
	<%= check_box_tag(:talent) %>
	<%= label_tag(:talent, "Run") %>
	</label>
  </div>
  
  
 <div class="form-actions">
    <%= f.submit class: "btn btn-success" %>
  </div>
  
<% end %>