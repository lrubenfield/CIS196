<%= form_for(@usertest) do |f| %>
  <% if @usertest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usertest.errors.count, "error") %> prohibited this usertest from being saved:</h2>

      <ul>
      <% @usertest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
