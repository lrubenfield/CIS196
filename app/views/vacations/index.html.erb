<h1>Listing vacations</h1>

<table>
  <tr>
    <th>Stop1</th>
    <th>Stop2</th>
    <th>Stop3</th>
    <th>Cost</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @vacations.each do |vacation| %>
  <tr>
    <td><%= vacation.stop1 %></td>
    <td><%= vacation.stop2 %></td>
    <td><%= vacation.stop3 %></td>
    <td><%= vacation.cost %></td>
    <td><%= link_to 'Show', vacation %></td>
    <td><%= link_to 'Edit', edit_vacation_path(vacation) %></td>
    <td><%= link_to 'Destroy', vacation, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Vacation', new_vacation_path %>
